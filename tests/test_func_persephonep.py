#!/usr/bin/env python
# coding: utf-8
#

from persephonep import func_persephonep

def test_check_url_protocol():
    return 0
    # mainPyQt5()
    # app = QApplication(sys.argv)

    # setWindowIcon is a method for QApplication, not for QWidget
    # path = os.path.join(os.path.dirname(sys.modules[__name__].__file__), 'icon_persephone.png')
    # app.setWindowIcon(QIcon(path))
    # ui = func_persephonep.PersephonepWindow()

    # assert ui.check_url_protocol('http://127.0.0.1') == True
    # assert ui.check_url_protocol('https://127.0.0.1') == True
    # assert ui.check_url_protocol('ftp://127.0.0.1') == True
    # assert ui.check_url_protocol('file://127.0.0.1') == True
